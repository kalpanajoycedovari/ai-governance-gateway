source:
  type: postgres
  config:
    host_port: "host.docker.internal:5433"
    database: openbank
    username: openbank
    password: "changeme_local_only"
    schema_pattern:
      allow:
        - "raw"
        - "marts"
        - "ml"
    include_tables: true
    include_views: true
    profiling:
      enabled: false

sink:
  type: datahub-rest
  config:
    server: "http://localhost:8080"